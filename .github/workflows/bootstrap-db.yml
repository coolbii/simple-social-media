name: Bootstrap DB

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install MySQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y mysql-client

      - name: Validate required secrets
        env:
          RDS_HOST: ${{ secrets.RDS_HOST }}
          RDS_PORT: ${{ secrets.RDS_PORT }}
          RDS_ADMIN_USER: ${{ secrets.RDS_ADMIN_USER }}
          RDS_ADMIN_PASSWORD: ${{ secrets.RDS_ADMIN_PASSWORD }}
          RDS_APP_USER: ${{ secrets.RDS_APP_USER }}
          RDS_APP_PASSWORD: ${{ secrets.RDS_APP_PASSWORD }}
        run: |
          set -euo pipefail
          for key in \
            RDS_HOST \
            RDS_PORT \
            RDS_ADMIN_USER \
            RDS_ADMIN_PASSWORD \
            RDS_APP_USER \
            RDS_APP_PASSWORD; do
            if [[ -z "${!key:-}" ]]; then
              echo "::error::Missing required secret: ${key}"
              exit 1
            fi
          done

      - name: Bootstrap database and app user
        env:
          RDS_HOST: ${{ secrets.RDS_HOST }}
          RDS_PORT: ${{ secrets.RDS_PORT }}
          RDS_ADMIN_USER: ${{ secrets.RDS_ADMIN_USER }}
          RDS_ADMIN_PASSWORD: ${{ secrets.RDS_ADMIN_PASSWORD }}
          RDS_DATABASE: ${{ secrets.RDS_DATABASE }}
          RDS_APP_USER: ${{ secrets.RDS_APP_USER }}
          RDS_APP_PASSWORD: ${{ secrets.RDS_APP_PASSWORD }}
        run: |
          set -euo pipefail
          DB_NAME="${RDS_DATABASE:-simple_social_media}"
          mysql \
            --host="${RDS_HOST}" \
            --port="${RDS_PORT}" \
            --user="${RDS_ADMIN_USER}" \
            --password="${RDS_ADMIN_PASSWORD}" <<SQL
          CREATE DATABASE IF NOT EXISTS \`${DB_NAME}\`
            CHARACTER SET utf8mb4
            COLLATE utf8mb4_unicode_ci;

          CREATE USER IF NOT EXISTS '${RDS_APP_USER}'@'%' IDENTIFIED BY '${RDS_APP_PASSWORD}';
          ALTER USER '${RDS_APP_USER}'@'%' IDENTIFIED BY '${RDS_APP_PASSWORD}';

          GRANT SELECT, INSERT, UPDATE, DELETE,
                CREATE, ALTER, DROP, INDEX, REFERENCES,
                CREATE ROUTINE, ALTER ROUTINE, EXECUTE
          ON \`${DB_NAME}\`.* TO '${RDS_APP_USER}'@'%';

          FLUSH PRIVILEGES;
          SQL

      - name: Verify grants
        env:
          RDS_HOST: ${{ secrets.RDS_HOST }}
          RDS_PORT: ${{ secrets.RDS_PORT }}
          RDS_ADMIN_USER: ${{ secrets.RDS_ADMIN_USER }}
          RDS_ADMIN_PASSWORD: ${{ secrets.RDS_ADMIN_PASSWORD }}
          RDS_APP_USER: ${{ secrets.RDS_APP_USER }}
        run: |
          set -euo pipefail
          mysql \
            --host="${RDS_HOST}" \
            --port="${RDS_PORT}" \
            --user="${RDS_ADMIN_USER}" \
            --password="${RDS_ADMIN_PASSWORD}" \
            --batch --skip-column-names \
            --execute="SHOW GRANTS FOR '${RDS_APP_USER}'@'%';"

      - name: Verify app user login
        env:
          RDS_HOST: ${{ secrets.RDS_HOST }}
          RDS_PORT: ${{ secrets.RDS_PORT }}
          RDS_DATABASE: ${{ secrets.RDS_DATABASE }}
          RDS_APP_USER: ${{ secrets.RDS_APP_USER }}
          RDS_APP_PASSWORD: ${{ secrets.RDS_APP_PASSWORD }}
        run: |
          set -euo pipefail
          DB_NAME="${RDS_DATABASE:-simple_social_media}"
          mysql \
            --host="${RDS_HOST}" \
            --port="${RDS_PORT}" \
            --user="${RDS_APP_USER}" \
            --password="${RDS_APP_PASSWORD}" \
            --database="${DB_NAME}" \
            --execute="SELECT 1;"
